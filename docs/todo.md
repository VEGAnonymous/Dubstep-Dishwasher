# >implying

---

- Working ML inference
- Fix `PitchShifter` and `Vocoder`
- `Serial` effect + `Parallel` nested chains and presets
- Dynamic resource usage tracking, potentially RT feedback in-app
- More effect modes / options (e.g. Freeverb instead of Datarro for `Reverb`, editable X-Shaper for `Distortion`)
- More effects (spectral, physical modelling)
- Stereo support (*highly unlikely due to hardware constraints*)

---